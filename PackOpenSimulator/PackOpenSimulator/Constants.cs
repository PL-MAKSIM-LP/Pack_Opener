using System;
using System.IO;

namespace PackOpenSimulator
{
    public class Constants
    {
        static public string projectDir = Directory.GetParent(Environment.CurrentDirectory).Parent.FullName;
    }
}
